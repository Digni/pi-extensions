{
  "name": "cyphant-pi-extensions",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Fabian's custom Pi extensions packaged for local and git installation.",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "keywords": ["pi-package"],
  "pi": {
    "extensions": ["./extensions"]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  }
}
